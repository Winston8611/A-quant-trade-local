# 模拟交易运行配置
market: CN_A_SHARE
mode: paper_trade
# 收盘后批处理时间（示意）
run_at: "15:10"
# 独立策略配置文件：改策略优先改这个文件
strategy_config: configs/strategies/ma_cross.yaml
date_range:
  # 回看历史区间（用于生成当前信号）
  start: "2020-01-01"
  end: "2025-12-31"
data:
  # 本地 DuckDB 文件路径
  db_path: data/db/market.duckdb
  # 交易标的列表
  symbols: ["000001.SZ"]
